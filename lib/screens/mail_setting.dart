import 'package:flutter/material.dart';

class MailSetting extends StatelessWidget {
  const MailSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
